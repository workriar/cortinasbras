# Configurações de Ambiente - Cortinas Brás
# Copie este arquivo para .env e preencha com suas credenciais reais

# Ambiente de produção (1 = produção, 0 ou vazio = desenvolvimento)
PRODUCTION=1

# Chave secreta do Flask (gere uma chave segura para produção)
# Exemplo: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=sua-chave-secreta-aqui

# Configurações de Email (Locaweb/SMTP)
MAIL_USERNAME=seu-email@seudominio.com.br
MAIL_PASSWORD=sua-senha-email
MAIL_DEFAULT_SENDER=contato@cortinasbras.com.br

# Banco de dados (MySQL em produção, SQLite em desenvolvimento)
# Formato: mysql://usuario:senha@host:porta/nome_banco
# Exemplo: mysql://cortinas_user:senha123@localhost:3306/cortinas_db
DATABASE_URL=mysql://usuario:senha@localhost/cortinas_db

# Porta da aplicação (opcional, padrão 5001)
PORT=5001
